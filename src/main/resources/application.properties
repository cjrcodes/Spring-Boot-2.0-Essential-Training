#H2 Config
#Output for test version

#logging.level.org.springframework.jdbc.datasource.init.ScriptUtils = debug

#Allows access the the console of h2, for viewing tables and data in an admin UI
#spring.h2.console.enabled=true

#Data source url for h2 console, making sure the local database is linked to the application properly
#spring.datasource.url=jdbc:h2:mem:testdb


#Postgresql Config

spring.jpa.database=postgresql
spring.sql.init.platform=postgres
spring.datasource.url=jdbc:postgresql://localhost:5432/dev
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.data.rest.base-path=api/rest
management.endpoints.web.exposure.include=*
management.endpoint.health.roles=USER
management.endpoint.health.show-details=when_authorized


#This setting can go on both
spring.jpa.hibernate.ddl-auto = none
